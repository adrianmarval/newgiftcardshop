# Implementation Tasks: sellFlow Improvements

- [ ] 1. Install `react-dropzone` — run `npm install react-dropzone @types/react-dropzone`. Acceptance: package.json updated, node_modules installed.
- [ ] 2. Create shared `ImageDropzone` component at `src/components/sell/image-dropzone.tsx` using react-dropzone. Props: `images`, `onAdd`, `onRemove`, `onClear`, `emptyLabel`, `emptySublabel`, `maxHeight`. Renders unified dropzone with thumbnails inside. Acceptance: component compiles, works standalone.
- [ ] 3. Replace dropzone in `ocr-entry-step.tsx` with `ImageDropzone` component. Remove the separate dropzone + thumbnail grid blocks. Acceptance: OCR step shows thumbnails inside drop area, drag-and-drop works, remove/clear work.
- [ ] 4. Replace dropzone in `proof-upload-step.tsx` with `ImageDropzone` component. Remove the separate dropzone + thumbnail grid blocks. Acceptance: Proof Upload step shows thumbnails inside drop area, drag-and-drop works, remove/clear work.
- [ ] 5. Add `resetToBrand()` action to Zustand store in `use-sell-flow.ts`. Resets step, entryMode, giftcards, images, unmatchedImages, lastRemovedCard. Preserves selectedBrand and selectedCountry. Add type signature to `SellFlowState`. Acceptance: action compiles, resets correctly when called.
- [ ] 6. Wire clean-slate navigation in `sell-batch-manager.tsx`. Add `handleBack(targetStep)` that calls `resetToBrand()` when target is 1, otherwise `setStep()`. Pass `handleBack` to step components via props or context. Acceptance: navigating back to Step 1 resets all state, mode selector reappears on next Step 2.
- [ ] 7. Remove legacy WRITE sites in `use-sell-flow.ts`. Update `updateGiftcard`, `acceptExtractedAmount`, `keepDeclaredAmount`, `confirmFuzzyMatch`, `setCardValidationResult`, `skipCardEvidence`, `resetValidation` to stop writing `validationState`, `extractedCode`, `extractedAmount`, `matchedImageId` flat fields. Acceptance: no legacy field writes remain.
- [ ] 8. Remove legacy READ sites in components. Update `proof-upload-step.tsx`, `review-step.tsx`, `sell-batch-manager.tsx` to remove `?? card.validationState`, `?? card.extractedCode`, `?? card.extractedAmount`, `?? card.matchedImageId` fallback chains. Acceptance: all reads use `card.evidence.*` only.
- [ ] 9. Remove legacy fields from `SellFlowGiftcard` type in `src/types/flows/sell-flow.ts`. Remove `validationState`, `extractedCode`, `extractedAmount`, `matchedImageId` and their `@deprecated` comments. Acceptance: TypeScript compiles without errors, no legacy references anywhere.
- [ ] 10. Create shared duplicate checker utility at `src/lib/duplicate-checker.ts` with `findExistingCodes(claimCodes: string[]): Promise<string[]>`. Acceptance: function normalizes, hashes, queries DB, returns duplicates.
- [ ] 11. Add `checkDuplicates` server action to `seller-actions.ts`. Input: `{ codes: string[] }`. Output: `{ duplicates: string[] }`. Uses `findExistingCodes`. Acceptance: action compiles, returns correct duplicates.
- [ ] 12. Integrate duplicate pre-check in `DetailsStep`. On "Continuar" click, call `checkDuplicates`, show blocking alert if duplicates found. Acceptance: user cannot proceed with duplicate codes, alert shows which codes are duplicates.
- [ ] 13. Integrate duplicate pre-check in `ReviewStep`. On "Publicar Lote" click, call `checkDuplicates` before `publishBatch`, block if duplicates found. Acceptance: publish is blocked with clear error if duplicates exist.
- [ ] 14. Fix Spanish labels in `DetailsStep`. Replace all English labels with Spanish equivalents per the spec table. Acceptance: all user-facing text in DetailsStep is in Spanish.
